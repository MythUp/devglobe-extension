<UserControl x:Class="DevGlobe.ToolWindow.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:vsui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
             Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
             Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}">
    <StackPanel Margin="12">
        <TextBlock Text="CONNECT TO DEVGLOBE"
                   FontSize="11" FontWeight="SemiBold" Margin="0,0,0,8"
                   Opacity="0.8" />
        <PasswordBox x:Name="KeyBox"
                     Padding="4,3"
                     KeyDown="KeyBox_KeyDown" />
        <Button x:Name="ConnectButton"
                Content="Connect"
                Margin="0,6,0,0" Padding="12,4"
                Click="ConnectButton_Click" />
        <TextBlock Margin="0,6,0,0">
            <Hyperlink x:Name="GetKeyLink"
                       RequestNavigate="GetKeyLink_RequestNavigate"
                       NavigateUri="https://devglobe.app/dashboard/settings">
                <Run Text="Get your API key on devglobe.app" FontSize="11" />
            </Hyperlink>
        </TextBlock>
    </StackPanel>
</UserControl>
